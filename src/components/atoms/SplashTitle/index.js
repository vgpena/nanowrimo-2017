import * as React from 'react';
import PropTypes from 'prop-types';
import './styles.sass';

export class SplashTitle extends React.Component {
  render() {
    return (
      <h1 class="SplashTitle">
        I have never been here before
      </h1>
    );
  }
}